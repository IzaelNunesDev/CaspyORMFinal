# caspy.toml

[cassandra]
hosts = ["127.0.0.1"]
port = 9042
keyspace = "caspyorm_app"

[cli]
# You can specify additional model search paths here, relative to the config file.
# Example: model_paths = ["my_app/models", "shared_models"]
model_paths = []
